# Canv.io API — dependencies

# web
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# config / validation
pydantic>=2.6
pydantic-settings>=2.2
email-validator>=2.1     # for pydantic EmailStr

# database
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9        # Postgres driver (used in Docker)

# security
passlib[bcrypt]>=1.7        # password hashing
pyjwt>=2.8                  # JWT access tokens
cryptography>=42            # Fernet encryption for stored Canvas tokens

# Canvas + ingestion (used by services, implemented in a later task)
httpx>=0.27
authlib>=1.3
pypdf>=4.2
python-pptx>=0.6
beautifulsoup4>=4.12

# AI generation (later task)
anthropic>=0.25
